CREATE VIEW student_records SELECT AS results.id,results.module_name,results.totalmarks,results.student_id,student.name FROM results,student WHERE results.student_id = student.id AND results.totalmarks>30;